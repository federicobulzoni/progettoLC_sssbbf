def easy ( ) : Int = {
    writeInt(3);
    if( 3 < 4){ 
        // solo con questo da errore
        return 3;
    } else {
        if (4 < 5)
            return 3;
        else {
            var b : Int = 3;
        }
    }
    //{
    //    // con questo non da errore
    //    return 2;
    //}
}
var z : Int = 1;