var x : Bool = True;
var w : Bool = 3;

def foo(a : Int)(b : Float, c : Bool) : Bool = {
    if(True && x || 3+3 && foo()){
        var y : Int;
    }
    while (3+3){
        if(foo(3)(3.3, True)+3){
            var x : Int;
            def foo() = {}
        }
        return 4;
    }
    var o : Int;
    var o : Int;
    def foo() = {}
    def foo() = {}
    def foo() : Bool = {}
    def foo(a : Int)(b : Float, c : Bool) : Bool = { 
        return 3+3;
    }
    x[True] = 3;
    foo();
    var z : Int;
}