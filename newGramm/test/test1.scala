def foo(a : Int)(b : Float, c : Bool) : Bool = {
    var a : Int;
    var o : Int;
    def foo() = {}
    def foo() = {}
    def foo() : Bool = {}
    def foo(a : Int)(b : Float, c : Bool) : Bool = { 
        return 3+3;
    }
}