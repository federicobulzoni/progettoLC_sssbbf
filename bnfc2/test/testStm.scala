//testStm

def fun () : Int = {
	do {
		x = 3
		y = 5
	}
	while (x > 1)
}


// a è un array di puntatori a interi.
var a : Int = * a (0)
// b è un puntatore ad un array di interi.
var b : Float = (*b)(0)
var c : & & Int = a(0)(1)

var d : & & Int = *& (a)(0)
(ELExp (
	LRef (
		LDeref (
			LArr (
				LIdent (PIdent ((18,23),"a"))
			) 
			(
				EInt (PInteger ((18,26),"0"))
			)
		)
	)
)
)
]
